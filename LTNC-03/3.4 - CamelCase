#include <bits/stdc++.h>

using namespace std;

int camelCase(string s) {
    int cnt = 1;
    for(char c:s) {
        if(isupper(c)) cnt++;
    }
    return cnt;
}

int main()
{
    string res;
    getline(cin, res);
    cout << camelCase(res) << endl;
    return 0;
}
